
name: Trigger Vercel Deploy Hook

on:
  push:
    branches:
      - master # chỉ trigger khi push vào main

jobs:
  vercel:
    runs-on: ubuntu-latest
    steps:
      - name: Call Vercel Deploy Hook
        run: |
          curl -X POST ${{ secrets.VERCEL_DEPLOY_HOOK}}
